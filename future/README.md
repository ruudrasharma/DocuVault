# future/

Files moved here are not wired into the live application but contain
potentially useful code for future development.

## ray_database.py

Not wired in. Contains a Ray-based parallel query helper for SQLite.
Revisit if/when the SQL layer needs horizontal read scaling.
